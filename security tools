security means >> following best practices & left shift security (finding the issue as early as possible)

pre-commit  >> in .git folder go inside hooks folder create file with name pre-commits  >> and then we paste the content like which wording should not b=be present in the
source code so that it cannot be commited 
chmod +x pre-commit

tokens, password, api key 

git leaks (if we use this no need to define conditions it automatically detects the sensitive data)

insatll git leak >> gitleaks delect >> gitleaks detect --no-git(its not going to go through complete git history)
gitleaks detect --no-git --report-path report.json  >> this commands scans the source code and saves the gitleak report in report.json format 
gitleaks detect --no-git --exit-code 1
gitleaks detect --source. -r b.txt
gitleaks detect --source. -r b.txt --redact
gitleaks detect --source. --path config.yaml
gitleaks detect --source. --exit-code 0
gitleaks detect --source . --verbose
gitleaks detect --source. -f json -r report.json

======================================================================
dependency scan >> in all below mwntioned files dependency details are present >> this checks the version of the dependency and is the mentioned version still safe to use
or is it defective its tells the version which we are currently using and version which not safe to use and tells us about version in which issue is fixed

dependency scans uses the databases >> NVD (national vulnerability database), Linux dependency data base, Redhat dependency database 

dependency scan tools >> Trivy, OWASP dependency check (only used in java based project, does not uses many databases)
it gets the list of dependency mentioned in pom.xml then it connects to the database  >> CVE_YEAR_SPECIFIC NUMBER  >> TRIVY is suitable for all kinds of language 
java >> pom.xml
nodejs >> package.json 
python >> requiremnt.txt
dotnet >> xyz.csproj

========================
trivy commands 

trivy fs >> file system scan
trivy fs .
trivy fs --scanner secret .
trivy fs --scanner config .
trivy fs --scanner vuln,secret,config .
trivy fs --severity HIGH,CRITICAL .
trivy fs --format table -o report.html .
trivy fs --skip-files README.md .
trivy fs --skip-dirs node_modules .
trivy image nginx
trivy image --format table -o image-report.html nginx:latest
==============================================================
source code for logic we used sonarqube which is also called for SAST (static application security testing)
SBOM >> software build of materials (used for  creating document about all the ingredeiants, dependencies, tools used in our project)
one dependencies may have sub dependencies 

================================================================================
kubernetes policy enforcing 
hardening >> i am  writing a yml mainfest file that we should follow the best practices  while writing it like defiining ram,cpu, probes, credentials should not be hardcoded
in that files.
RBAC >> role based access control >> we deploy application in pod so pods are communiting with each other as they require volume so we need to look that they sholud not give
rootv level access >> service account and admin credentials, common users (deployment,modify)
in RBAC we create the service account and give it persmission also attach the resources (minimal priviliges)

===============================================================================
IAC scan 

there could be issue in the terrform config files like keeping resource public for this we use tools like checov they are issues or misconfiguration
checkov can be used for >> kubernetes, docker, secrets, ansible 
checkov -d .  >> used to scan current directory
CWK_AWS_130 >>  checkov rules
checkov -d . --download-external-modules true 
chexkov -d . --framework terraform
checkov -f main.tf
checkov -d . --frameowrk kubernetes
checkov -d . --check HIGH,CRITICAL 
checkov -d . -o json
==================================================================================
vault  
previously we were using secrtes file for creds which were encrypted in base 64 format and then we were storing it in secrets file but any one can decrypt it 
as its only base 64 encrypted
to resolve tis we will save the creds in hashicrop vault at time of deployment the kubernetes will call hashi crop vault for creds
secret manager, key vault 
===================================================================================
DAST >> dynamic application security testing 
OWASP ZAP >> used to find out if the running application can be hacked or attacked from the outside 
MITM (MAN IN THE MIDDEL) >> OWASP ZAP 

PC >> OWASP ZAP >> K8s application server 
it can directly attack the application over the network >> performs the attacks on the websites 
OWASPS ZAP have top 10 >> its list where its have top 10 most common yet very powersful misconfiguration or issues which may be avialble in your code 
=================================================================================
prowler >> cloud platfrom security tool

security best practices, audits, inicdent response, continuous monitoring , hardening, forenscis readiness

dockle >> used to build best practices for docker image, build secure docker images 
dockle sonarqube:lts-community

dockle -f json -o results.json
==================================================================================

SBOM >> software build of materials (conatins list of software components, librarberes, dependencies) that are utilized in the project 
generate the SBOM file, c=scan vulnerabilities, attesting the files (signing the files digitally), verifying cosign
list which contents the software compononents (name, version,license, source), dependencies, libraries
should be created in the specific format scanning the SBOM file
store the SBOM file on any of the location
before sharing the file with clienty we should sign it.

report for SBOM
cyclone DX >>cyclone dependency exchange focues on security created by OWASP  >> CLI tool is used 
SPDX >> software package data exchange developed by linux foundation focues on licenses and metadata  >> managed by compliance team

Trivy can generate the SBOM report and can scan the report >> used when quick scan is required
syft >> generate the SBOM report
grype>> scans the SBOM report 
cosign >> used for testing & verifying the SBOM report
for syft and grype >> dependencies sholud be installed earlier

trivy fs . --format cyclonedx --output trivy-enriched-sbom.json
trivy sbom trivy-enriched-sbom.json --format table
trivy fs . --scanners vuln,license,secret
========================================================================

OWASP top 10 >> 2021  now latest is 2025
means, why, tech,affect, how to secure

broken access token  >> authentication and autherization (happens becouse developer checks login but forgets to give permission, backend trust traffic frontend,no ownership check in API
role checks and RBAC  what ever request user is sending it is valid 

in AWS iam role id created and full permission is gien to the user, s3 bucket is public, pods using the cluster admin role 
how it affects >> data leaks, account takens, compliance 
prevent >> authorization checks on backends, verification to user id, roles, ownership, RBAC, never trust frontend traffics

cryptographic failures >> senstivie data like api keys, crfeds, token are easyly readable saved in plain tgext, not using https, hardcoing the keys

username can be saved in plain text but the password should be encrypted, in cloud project realtyed secrtets openly stored in github, no encryption on volumes, 
affects >> password reuse, creds stuffing, data thef, leagel fines

use https always using gateway API, encryption, use hashicrop valuts, rotate secrets regularaly.

injections  >> verifying the inputs given by users to the code, running users inputs as command 

SELECT * FTOM users WHERE username = '$input'; >> what kind of users should added should be defined (code should be written to verify the input)
data liks, authentication, data delection, remote code execution 
preventation >> verification should of proper format, least privileges, data must be data 

insecure design >> design of application is not good 
cloud >> no rate limliting on APIs, no quota on SAAS, No protection againats abuse
finanicaial fraud, infra cost, ligal consiuses
security reviews, limits everywhere

security misconfiguration >> spplication running on defaults security nothing is hack but any one can hack it as its open to use
keeping port range open,debug mode on all logs can be easily avilable, keeping enverionment varibales in source code

debug is true, stack traces are exposed, env variables are leaked, internal path 
admin panel of application is accessible to everyone 
public s3 bucket, k8s dashboard is exopsed, prometeheus and grafana re kept open, security group is allowed anywhere from intetnet

sensetive data,  easy entry poinyt for attackers,

disbale debugs close unused ports, continuos security scanning, DAST : OWASP zap
vulnerable and outdated componenets >> libraries and dependencies are outdataed are vulnerable are used in our application 
dependencies, libraries never got updated, vulnerablites scanning not done, 

log4j vulnerability >> use SBOM list of all dependencies, libarabies, docker images are used
full server compramise, data theft, mass exploilation  >> regular 
SBOM, Trviy, Synk

identification and authantication failures >> system not verifyring the login, seesion time, weaak passsword, no MFA
min length
AWS no MFA, shared credentials, hardcoding the api keys
account takeover, unauthorize action, token rotation, limiting the login attempts,
authentication failures leads to account take over.

software and data integrity failures  >> blindly trusts the code never verify its  building application with jar system assumes that the code is safe 
build app jar >> docker >> deployment k8s if somne one replaced the jar file and they put the malious jar file then it will be blunder
checksum  and signature 
massive ammount of issue 
sign all arifacts and imagesverify checksum 
restrict who can modify >> this can reduce the software and data integrity failure 

giving the fingerprint vaildation to the jar file or docker images 
signing the docker image or jar 


security logging and monitoring failures >> if we cont have monitoring then there will be no alterts and monitoring like attack happening 
attacker can try thousnad passwords we should get the alerts once any thing wrong is happening 
root access to the account
intergate the monitoring tool keep log authentication, centralized loggig, alerts on anything suspesious happens


server side request forgery (SSFR)  >> attackers mades the server yo attack on there behalf cannot be reached from the outside 
sending the url as the part of the application 

AWS >> 169.254.169.254 >> SERVER (where peace of code is written) >> in this case appliaction itself runs the culprit url and saves the output in itself
theft of cred, account access gain, infra taken over, 
valiadte the url, block internal ip ranges, disvbale the metadata access, block network level projections 

WebGoat, burbsuite(this is the tool which helps to get access to webrequest which can be modified leter on), firefox 

highjack cookie >>> some number will be missing and create the dummy conetent and paste it there >> trying the generate the particulsr attacks >> and start attacking 

owasp Zap >> DAST & SAST 

dynamic application security testing >code has been deployed and application is running

OWASP ZAP : becomes attacker, after deployment also we have do the security checks 
requests and responses are there 
browser   >>  ZAP >> Application 

ZAP >> can record traffic, analyise, replay, modify, attack our website also >> we are going to do this into staging environment then we can relese the application

it can act as intercepting proxy >> MAN IN THE MIDDLE

FULL URL + query parameter, Headers(auth+cookies+content-type), body, response headers, cookies, tokens, status codes and error messages

200, 503, 404  >> zzap will act as cctv camera wathcing over everything 2

setup site tree + history >> prepare the tree (where you went it will  create the tree) history (request + response are captured by zap) and data avilable for analysis

crawling similar to snake >> explore the webpages by itself >> traditional spider crawling & AJAX spider
traditional >> zap reads the html pages, forms,links but it can clik the website or modify it can only read  its good for basic websites
AJAZ spider >> can click buttons, integrate the java code, execute the code, new endpoints discover but its going to be slow >> can be used for the complex website 

scanning >. passive scaning and active scaning

passive >> analysis of traffic which is genereted by others
active >> in this case ZAP it self sends the traffic it attack the website in different ways
