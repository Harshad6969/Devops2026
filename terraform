4 Types of issues:

a. Language Errors>> Missing braces, wrong indetation, Typo.  HCL (declrative configuration language approach).
b. State errors >> Drifts (Manually changes something managed by terraform), deleted or moved state.
c. Core errors >> Bugs in Terraform, Dependency graph issue. (issue in terrform inbuild code)
d. Provider errors >> invalid creds, API limits/Timeouts, Wrong arguments in the resource (autheication with the aws and azure then its provider task to make communication)

terraform fmt: formats terrfaorm configuration files to canonical styles 
terraform validate: performs static validation of terraform configuration syntax and structure

srtring interpoliation

tags = {
  Name = $var.name-learn    #this is not correct one
}

  Name = "${var.name}-learn"

#terraform fmt
#terraform init
#terraform validate
cycle error >> highlights the mutual dependency between two resources (instances of circular logic in terraform dependency tree) as both resources are dependenet on each other
terrofrom gets confused like which resource to create first so we need to break the dependency 
we will create the dependency in such a way that terraform create the resources

invalid referenece >> slpat expresssion it only passes list types
missing resource instance key >>


command to create the logging mechnaism for 
export TF_LOG_CORE=TRACE
export TF_LOG_PROVIDER=TRACE
export TF_LOG_PATH=logs.txt

command to remove the logging mechnaism 
export TF_LOG_PROVIDER=
export TF_LOG_CORE=

#terraform console   >> command used to get the output in fastest manner 

use of local block is to define the name vaiables, we can refer the local block in the root module but variable can be provided in differenet module.

what is for_each expression in terraform for_each is meta argument or generic expression 
terraform provides some arguments which can be used in the script like if we want to use the multiple instance of same resource

terraform atlantis, terraform grunt, terraform cloud 

terraform cloud, terraform secret

drift in terraform

Ansible (playbooks and dynamic inventory file), vault roles, project (5 VMs) 
installing java, maven & docker, clone project (exit,pull), build (logs, errorlogs) stdout, stderr, build, login, pushing 
we will be using dynamic inventory, roles, vault

dynamic inventory >> similar tag for the vm which are going to be used in the multiple vms for there reference
vault  >> saves sensitive data which can be used in playbook it has its own vault
ansible-vault create vault.yaml (user/password)
roles >> roles are like resuable automation it will be like pacjage. repository which can be used to build specific task. it also contains 
anisble galaxy  >> similiar to helm cart which are like package and written in variable format which can be used at the time of installation of software.
we get here the pre created roles and collections 
anisble collection
